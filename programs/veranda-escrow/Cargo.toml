[package]
name = "veranda-escrow"
version.workspace = true
edition.workspace = true
description = "Veranda escrow / commitment / disclosure Anchor program"
license.workspace = true

[lib]
crate-type = ["cdylib", "lib"]
name = "veranda_escrow"

[features]
default = []
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]

[dependencies]
anchor-lang = { workspace = true, features = ["init-if-needed"] }
anchor-spl = { workspace = true }
solana-program = { workspace = true }
sha2 = { workspace = true }
# Groth16 verification (membership + reveal proofs).
# The arkworks deps below are referenced from `services/zk_prover.rs` (backend)
# and the on-chain verifier in `instructions/reveal_identity.rs`. We currently
# stub the verifier; uncomment when the real `alt_bn128_pairing` impl lands.
# ark-bn254 = { workspace = true }
# ark-groth16 = { workspace = true }
# ark-serialize = { workspace = true }
# ark-ff = { workspace = true }
# ark-ec = { workspace = true }
# ephemeral-rollups-sdk = { workspace = true }
