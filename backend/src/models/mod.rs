// These structs are domain models used by future endpoints not yet wired
// to the matching pipeline. Marked `allow(dead_code)` until then.
#![allow(dead_code)]

pub mod agent;
pub mod candidate;
pub mod match_record;
pub mod user;
